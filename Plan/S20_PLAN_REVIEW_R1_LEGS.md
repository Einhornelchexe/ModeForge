# S20 Plan-Review — Runde 1, Drei-Legs-Archiv (verbatim)

**Datum:** 2026-08-23 (Läufe 2026-08-20/23).
**Status:** abgeschlossen; adjudiziert in `Plan/S20_V2_HARDENING_PLAN.md` §7 (kanonische Reihe R-01..R-45).
**Gegenstand:** PLAN-v1 (`S20_V2_HARDENING_PLAN.md`, v1-Body §0–§6).
**Zweck dieses Archivs:** Auditierbarkeit der Severity-Zuordnung (§8 R-57) — die Leg-Reports im Wortlaut, wie sie beim Architekten ankamen. Peer-Brief identisch für alle Legs (neutral, anti-anchored); Orchestrierung direkt durch den Architekten (Leg-Outputs verbatim eingesammelt, keine färbende Zwischeninstanz — dokumentierte Abweichung vom Frisch-Orchestrator-Muster des architektur-plan-Skills).

**Severity-Rekonziliation (R-57):** Leg-HIGHs, die kanonisch nicht als HIGH geführt werden: Gemini F-1 (HIGH) → R-11 (MED): identisch mit Opus O-R11; Einordnung als Scheduling-Regel-Fix, kein Bau-Blocker per se. Codex C-R03 (HIGH) → R-27 (LOW): Depends-on-Ergänzung, Einzeiler. Codex C-R02 (HIGH) → R-11 (MED): dieselbe Kollision. Codex C-R11 (HIGH) → R-05 (HIGH, unverändert). Codex C-R24/C-R25/C-R26 (HIGH) → R-04/R-03/R-09 (HIGH, unverändert). Codex C-R14 (HIGH) → R-16 (MED in Opus-Reihe; per §8 R-46 auf HIGH-Niveau nachgeschärft). Maßstab: Severity bewertet den Schaden im Plan (würde die Stufe scheitern/falsch bauen), nicht die Formulierung des Legs.

---

## Leg 1 — Codex gpt-5.6-sol xhigh (Routing: CLI-Echo; „Gemini did not run because WSL access was denied" bezieht sich auf die Codex-Sandbox, nicht auf Leg 2)

Model actually run: `gpt-5.6-sol`, reasoning effort `xhigh`.

| ID | Severity | Anchor | Defect | Recommendation |
|---|---|---|---|---|
| C-R01 | HIGH | §6; git status; git ls-files | The per-stage commit/rollback model has no tracked baseline. The tree currently has 25 modified and 53 untracked entries; all packages/image sources and image tests are untracked. A stage commit would absorb whole S18 files, and reverting it could delete the baseline rather than only the stage change. | Add a Stage 0 that records the complete S18 baseline in a clean commit/worktree before A–K. |
| C-R02 | HIGH | §0; I-7; §2; §6 | The collision claims contradict the schedule. B changes an existing constant in thresholds.ts while D3 may run concurrently there; A/B and C/G also share i18n.ts. "Append-only" does not prevent Git conflicts or establish attribution, and rollback incorrectly calls lanes 2/3 file-disjoint from lane 1. | Serialize every shared-file stage or use isolated worktrees with a named integration owner; encode B↔D3 exclusion in the DAG. |
| C-R03 | HIGH | §2 | The executable dependency graph permits K after I→J while C, D1, D2, G, or H remain unfinished. J also describes G's drawing mode but depends only on I. | Add an all-code barrier: J must depend on G and I; K must additionally depend on H, D1, and D2. |
| C-R04 | HIGH | §6 | The named scratchpad/opus-codex-verify* oracle corpus does not exist in this checkout. Consequently, the principal calibration and fail-before/pass-after gates for A/B/D2/D3/E/F/H cannot be rerun or audited. | Commit deterministic reproductions under a versioned test/repro path, with exact commands and fixture hashes, before implementation. |
| C-R05 | HIGH | Triage §7/§9; Plan §3 B; §4(b) | B does not resolve the agreed V5a P0 blocker. Its acceptance explicitly keeps the −39% to −40% halo cases released and adds only an informational warning, whereas the agreed fact base calls for fail-closed behavior. | Add an independently discriminating gate or obtain explicit operator adjudication changing the release contract; otherwise mark B partial, not P0-complete. |
| C-R06 | HIGH | §3 A; §4(a); aperture.ts:1226-1227 | Masked-vs-full moments of the fitted Gaussian are not "the error itself." They measure sensitivity conditional on a model fitted to already-masked data. Missing pixels aligned with an unmodelled halo/asymmetry can hide the residual and bias while the model-based estimate stays small. | Treat it as model-conditioned sensitivity and add a conservative structured-loss arm unless bounded on releasable halo, wing, super-Gaussian, asymmetric, slope, and residual fixtures. |
| C-R07 | MED | §3 A; §4(a) | finiteFraction = 0.5 has virtually no margin against the sole 49.7%-dead random fixture, while the case motivating this arm has only about 5% finite coverage. Seed and geometry variation are uncalibrated. | Sweep many masks/geometries and choose the fraction from a confidence bound; add threshold-boundary tests. |
| C-R08 | MED | §3 A; §4(a); thresholds.ts:11 | One NaN can trigger model rasterization and two moment passes over a permitted 4096² ROI. No maximum-frame runtime or memory acceptance exists. | Require a streaming/ROI-local implementation and pin peak memory/runtime for a maximum-size early-gates-passed frame containing one NaN. |
| C-R09 | HIGH | §3 C; main.ts:1638,1853-1861; decode.ts:245-253 | Removing the dtype check accepts uint32 dark frames, but Array.from(decoded.pixels) does not apply float32 rounding while the main image uses Float32Array.from. Above 2²⁴ the two lanes subtract numerically different representations despite both being labelled float32. | Cast the dark through Float32Array.from before storage, or restrict accepted dark dtypes; add a uint32-above-2²⁴ parity oracle. |
| C-R10 | MED | §3 C; views/image.ts:1267-1274 | C cannot deliver its promised split error messages with its declared scope. The view ignores darkError and always renders the generic imgBgDarkMismatch; views/image.ts is not owned by C. | Add views/image.ts, represent the error as a typed kind, and test decode failure and dimension mismatch separately in both languages. |
| C-R11 | HIGH | §3 D3; roi.ts:170-176 | The V6a "fix" is behaviorally identical to current code: exact size/peak ties already retain the smallest seed through ascending scan order. It does nothing about the verified 1e-9 peak perturbation, and no V6a acceptance test exists. | Define a relative/absolute peak-equivalence tolerance, then pin ±1e-9 perturbations and a materially brighter-component control. |
| C-R12 | LOW | §3 D1; decode.ts:161-182 | Orientation is to be consumed without adding tag 274 to the decoder's type validation, violating the existing "every consumed tag has its documented type" invariant. | Add expectTagType(274, "Orientation", [3]), validate count 1, and add malformed-type/count fixtures. |
| C-R13 | MED | §3 D2; fit.ts:1021-1029,1070-1078 | The new relative-step constant has no value or calibration rule, and the implementation does not retain lastAcceptedRelParam. Its 618-scene oracle is also among the missing scratch scripts. | Pin the constant and storage semantics, and commit both a boundary fixture and the complete sweep before D2. |
| C-R14 | HIGH | §3 E; background.ts:296-350,617-621 | The proposed sqrt(n/(n−3)) correction cannot meet its own acceptance criterion: multiplying a −24.5% n=16 estimate by sqrt(16/13)=1.109 still leaves roughly −16.3%. Moreover, the code uses MAD/IQR after IRLS, not OLS residual RMS for which that DoF formula applies. | Replace it with a correction justified for the robust estimator—such as leverage-adjusted/leave-one-out residuals or calibrated correction tables—and settle it before build. |
| C-R15 | MED | §3 E; background.ts:286-335,694-700 | "Force scaleSource: floor" does not define the resulting sigmaCounts; for float32 n≤2 the floor can be zero. It also leaves a one-pixel median applied as the actual background offset, so C3 is only relabelled, not prevented. | Define an explicit insufficient-sample outcome: reject/fallback the background method or export an unavailable scale. Pin corrected pixels, method, sigma, source, and warning. |
| C-R16 | MED | §3 E | The C5 trend warning has no statistic, significance threshold, geometry requirements, or negative controls. An always-on warning for multi-rectangle inputs would pass the sole positive fixture. | Specify the normalized trend statistic and threshold; add flat-noisy, unequal-rectangle, harmless-offset, beam-tail, and true-gradient cases with a false-positive budget. |
| C-R17 | HIGH | §3 E; packages/core/src/warnings.ts; i18n.ts:468-519 | E introduces IMAGE_BACKGROUND_GRADIENT_IN_REFERENCE but omits both the core warning-code union and UI title records from its scope. A normal implementation will fail typecheck or display an untranslated raw code. | Add packages/core/src/warnings.ts, apps/web/src/i18n.ts, and warning-title tests to E's ownership and collision schedule. |
| C-R18 | MED | §3 F; aperture.ts:693-705,1230-1240 | The 3×3-median peak has no boundary, non-finite, or stage-B-field definition. F follows A, which intentionally releases some heavily random-masked frames; an undefined NaN-neighbour policy can then collapse the peak ceilings and undo A's acceptance. Narrow σ≈1.5–3 beams are also outside the pinned 11×6 curve. | Specify finite-neighbour and edge semantics; rerun all A coverage fixtures after F and add narrow, subpixel, border, and NaN-neighbour release pins. |
| C-R19 | MED | §3 B; analyze.ts:461-493 | tierCheck has three unavailable branches, but acceptance covers only invalid Stage A. Incorrect reason mapping or non-null gaps/thresholds with evaluated=false would pass. | Use a discriminated union and test all unavailable reasons, evaluated-below-threshold, and disagreement cases. |
| C-R20 | MED | §3 A; §3 I/J; views/image.ts:512-523; docs/theory/image_analysis.md:74-80,160 | The new suppression reason is absent from the manual localized label map, and A excludes views/image.ts. Existing documentation will also retain the false "every statistic ignores non-finite values" claim and stale gate precedence; neither I nor J explicitly assigns those edits. | Add localized reason keys and view mapping to A, and explicitly update the existing gate list and warning catalogue—not only new §13 prose. |
| C-R21 | MED | §3 G; main.ts:375-386; state.ts:351 | The default analyzed view is close-up, so corner background rectangles can be outside the displayed/exported canvas and remain invisible or uneditable. G does not define a view-mode transition. | Force or explicitly offer full-sensor view in background-rectangle mode; test analyzed close-up → mode switch → visible/editable corners and PNG export. |
| C-R22 | MED | §3 G; main.ts:766-865,3502-3578 | Acceptance tests only creating a fifth rectangle. It does not pin selection state, moving/resizing existing rectangles, overlap precedence, active-index removal, or reset after changing background method. The requested editor can pass while only "create" works. | Define persistent active-index/drag-target semantics and add e2e coverage for select, move, edge/corner resize, overlap, removal, and mode reset. |
| C-R23 | MED | §3 G; views/image.ts:73-100; analyze.ts:135-185 | buildAnalysisSummaryJson(result) cannot export background rectangles because neither its parameter nor ImageAnalysisResult contains them. The plan leaves this as an in-stage check and acceptance never tests it. | Pass an explicit export context containing bgRects, update the callsite, and pin exact JSON round-trip without reading global state. |
| C-R24 | HIGH | §3 H; main.ts:2907-2922 | Applying the 0.85 non-shrink clamp to full-frame mode blocks the normal first full-frame→compact-ROI action: a healthy beam ROI is ordinarily far smaller than 85% of the sensor. That directly contradicts the healthy-beam acceptance. | Retain the full-frame exemption after the geometry guard, or replace it with a degeneracy-specific size/geometry test; add a healthy compact full-frame fixture. |
| C-R25 | MED | §3 H; views/image.ts:1298-1302 | The premise that the button will disappear is false. Rendering checks only gauss2d.converged || releasedValid; a rejected pure-noise geometry still displays the button, and H excludes the view. | Add views/image.ts to H and use a shared eligibility result for rendering and click handling. Test button absence, not merely a no-op click. |
| C-R26 | HIGH | §3 F; §3 H; main.ts:5-15; packages/api/src/index.ts:50,86-88; check-scope.mjs:61-66 | Exporting fitGeometryIsReleasable only from packages/image does not make it available to the web app. The web imports physics exclusively through packages/api, and direct image-package imports are forbidden. Neither F nor H owns the required API re-export. | Add packages/api/src/index.ts to F/H scope and re-export the predicate there, with API and scope-gate tests. |
| C-R27 | MED | §3 D3; §3 E; §3 I | E intentionally changes σ_B, while suggestRoi consumes σ_B. No post-E integrated oracle repeats D3's apply-suggestion→0/15-clipped requirement, so D3 can regress after its own stage passes. | Re-run the complete D3 suggestion/apply corpus after E for none, rect-median, and robust-plane paths. |
| C-R28 | MED | §4(f); roi.ts:118-136 | The padding inversion uses the raw component maximum. A connected hot pixel increases peak/threshold, reduces the inferred σ, and can recreate aperture_clipped; F's robust-peak work does not affect suggestRoi. The candidate √e guard and the stated 6.25σ acceptance are not calibrated to the formula's nominal 7.5σ target. | Use a robust component peak or explicitly cap peak influence; pin hot-pixel, rotated, flat-top, subpixel, and guard-boundary cases with exact expected padding. |
| C-R29 | MED | §3 I; I-6; §6 rollback | A/B/D3/E/F are separate HOLD commits, but behavior-derived test pins and reference artifacts are deferred to I. The plan does not state whether intermediate commits may leave verification red, undermining independent rollback and stage approval. | Require every behavior stage to update and pass its directly affected tests/artifacts; reserve I for integrated cross-stage baselines and documentation. |
| C-R30 | MED | §3 K; package-lock.json:1-9 | The claimed complete version inventory omits package-lock.json, whose root versions are still 1.0.0. Searching only for old 1.1 strings lets K pass with inconsistent 2.0/1.0 metadata. | Include authoritative lockfile fields and validate that all designated release-version artifacts equal 2.0.0. |

Verdikt: NEEDS-REVISION.

---

## Leg 2 — Gemini-Leg (Routing-Selbstauskunft: Gemini 3.1 Pro (High))

**1. Premise Verification (Spot-Checks):** aperture.ts:1162-1170 (TRUE), :1192-1196 (TRUE), analyze.ts:333-397 (TRUE), aperture.ts:617-618/641 (TRUE), analyze.ts:1130 (TRUE), main.ts:1638/1848 (TRUE), background.ts:674-676 (TRUE), decode.ts:245/170-182 (TRUE), background.ts:704-727/462-464 (TRUE), roi.ts:100-150 + fit.ts:1070-1078 (TRUE).

**2./3. Stage Cut / Design Decisions:** „highly modular"; §4(a) „exact mathematically honest way"; §4(b) „data proves a flat threshold cap destroys the yield"; §4(c) sauber; §4(d) „correct epistemic fix"; §4(f) „perfectly compensates … implementation trivial".

**Findings:**

| ID | Severity | Anchor | Defect | Recommendation |
|---|---|---|---|---|
| G-F1 | HIGH | §2 Lane-Struktur / I-7 | File-collision risk on thresholds.ts: Stage B modifies a constant (exclusive lock per I-7) while Stage D3 (Lane 2, parallel) performs additive writes to the same file. | Make D3 depend on B, or move D3 into Lane 1. |
| G-F2 | LOW | §4(f) | The padding formula misses that threshold might be Infinity/NaN if malformed sigmaCounts reaches it. | Strict finiteness check for sigmaEst with fallback to the 8px default. |

Verdikt: NEEDS-REVISION (F-1).

---

## Leg 3 — frisches Claude-Opus-Agent-Leg (Routing: Claude Opus 5, Agent-Tool, Session-Effort; read-only; ~35 gezielte Reads/Greps + Closed-Form-Checks)

O-R01..O-R30 — vollständige Tabelle und „Notes on what checked out" sind wortgleich als kanonische Reihe R-01..R-30 in `S20_V2_HARDENING_PLAN.md` §7.1 adjudiziert; der Original-Report umfasste zusätzlich die verifizierte Positivliste (alle übrigen Anker TRUE) und das Verdikt NEEDS-REVISION („five HIGH findings … plus systematic gaps in calibration budgets, cross-stage artifact ownership, and suite-redness declaration"). Kern-HIGHs: R-01 finiteFraction-Messerkante (0.4997 vs. 0.5), R-02 3×3-Median skalenblind (0.6065·A bei σ=1), R-03 Button-Render-Prämisse falsch, R-04 Vollframe-Klammer-Addendum blockiert Kern-Workflow, R-05 V6a-No-op, R-06 Gate-Kalibrierung aus einer Szene ohne Clean-Budget.

**Doc-Version:** 1.0, 2026-08-23.
