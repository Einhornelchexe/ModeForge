# S20 Plan-Review — Runde 2 (Delta auf §7), Zwei-Legs-Archiv (verbatim)

**Datum:** 2026-08-23.
**Status:** abgeschlossen; adjudiziert in `Plan/S20_V2_HARDENING_PLAN.md` §8 (R-46..R-61); Exit-Gate §8.4.
**Gegenstand:** PLAN-v2 (v1-Body + §7, §7.4-Entscheide eingetragen). Betreiber-Zuschnitt: zwei Legs („ein opus xhigh und ein terra xhigh").

---

## Leg 1 — Codex gpt-5.6-sol xhigh (Routing: CLI-Echo)

| ID | severity | anchor | defect | recommendation |
|---|---|---|---|---|
| CX-D01 | HIGH | §7 R-16; background.ts:486 | The proposed trace correction is algebraically identical to √(n/(n−3)): the present OLS leverages sum to exactly 3. For n=16, the factor remains 1.1094 and the measured −24.5% estimate remains about −16.2%, violating the required ≤5% residual. | Specify a genuinely different, robust-scale correction—such as a validated influence-function or held-out residual estimator—and pin its exact algorithm and fixtures. |
| CX-D02 | HIGH | R-32; §7.3 | Stage 0b cannot be executed as a "port": all three scratchpad/opus-codex-verify{,2,3} source trees and the destination agents/verification/s20-repros/ are absent from the workspace and Git history. Also absent from 9db4fbd, despite "Teil der Baseline." | Recover the exact scratch artifacts and define 0b as a separate post-0a commit. If recovery is impossible, specify independently validated replacement fixtures rather than a port. |
| CX-D03 | HIGH | R-17; R-37 | The revisions conflict: R-17 retains plane correction and permits RangeError only for geometric failure, while R-37 requires minimum-sample failure to reject the method and fall back to none. R-16 simultaneously requires calibration of the single-2×2 fixture that R-37 would reject. | Add a method × sample-count truth table covering resulting method, corrected field, sigma/source, warning, and fallback; then align R-16's fixtures and acceptance oracle with it. |
| CX-D04 | MEDIUM | R-11; R-43 | D3 and E may start concurrently after B, but E now owns a post-D3 corpus rerun. E cannot reach HOLD if D3 has not landed yet. | Make E depend on D3, or move R-43 to a checkpoint that explicitly depends on both stages. |
| CX-D05 | MEDIUM | R-06; Stage A | "Clean maximum × margin" does not define the negative calibration population. Unmasked members of the 74-case clean set never enter the coverage path, while the plan does not state which masked variants are benign calibration negatives versus suppression positives. | Define a benign masked population across clean families, its confidence/margin rule, and required separation from random-50% and the smallest structured-mask positive. |
| CX-D06 | MEDIUM | R-36; state.ts:113 | Stage C requires a typed dimension/decode darkError, but ImageTabState.darkError remains string|null and state.ts is not added to C's scope. | Add apps/web/src/state.ts to Stage C and define the discriminated error contract explicitly. |
| CX-D07 | MEDIUM | R-03; Stage H | R-03 requires a new "button absent on pure noise" oracle, but H's revised scope adds views/image.ts without adding an owning test file. | Add tests/unit/image-view-functions.test.ts and any required pure-noise fixture/e2e file to H's scope. |
| CX-D08 | LOW | document header; v2 footer | The header still says PLAN-v1 and "Cross-Review ausstehend," while the footer says PLAN-v2 final. The cascade makes the footer authoritative, but the document remains internally contradictory. | Update the header/status or explicitly label it as the preserved v1-body header superseded by §7. |

Requested spot-checks that passed: R-09 fits the current ImageAnalysisResult/fits construction (additive fits.gauss2d.geometryReleasable feasible without changing packages/api); R-13 correctly avoids the Stage-B field (NaN-masked exterior at aperture.ts:186-199); 9db4fbd exists at HEAD/origin/main (87 files, +28,842/−89, all 17 tracked packages/image paths, seven Orchestrierung memos and two transient root scripts excluded); R-33 closed consistently by §7.4 option (a); before/after working-tree snapshots match.

Verdikt: NEEDS-REVISION.

---

## Leg 2 — frisches Claude-Opus-Agent-Leg (Routing: Claude Opus 5, Agent-Tool, Session-Effort — kein xhigh-Regler verfügbar, vermerkt; read-only; voller PLAN-v2-Read + ~22 Code-/Git-/Artefakt-Verifikationen)

| ID | Sev | Anchor | Defect | Recommendation |
|---|---|---|---|---|
| OP-D01 | HIGH | §7.1 R-17 vs §7.2 R-37; background.ts:694-702 | Two accepted revisions prescribe opposite behaviour for the same input (R-17 "Ebenen-Korrektur bleibt erhalten" vs R-37 method-degradation to none; both claim to replace E(b); R-17's one-regime claim abolishes the only reconciliation). | Pick one regime: n < BACKGROUND_MIN_REFERENCE_SAMPLES → method degrades to none AND carries scaleSource:"floor" + IMAGE_NOISE_SCALE_SUSPECT; strike R-17's plane-retention clause. |
| OP-D02 | HIGH | §7.1 R-16; background.ts:493-509, ~:620 | The "trace-based correction" is algebraically identical to the v1 formula: Σh_i = 3 exactly (also for the weighted hat matrix); code computes only maxLeverage over the UNWEIGHTED design and never a trace; the genuine mismatch (MAD/floor estimator on IRLS-Huber residuals, not RMS) is untouched by any dof formula. | Strike the tr(H) expression; make the empirical clause binding; name the correction as a fitted c(n) or a Huber-ψ effective-df κ. |
| OP-D03 | HIGH | §7.1 R-08 + §7.2 R-36 + §7.3; §2 row C; §1 I-7 | New file-exclusivity violation created by the revisions: views/image.ts is put into Stufe A (R-08) and Stufe C (R-36) while §2 permits A ‖ C; the file is not on the I-7 exception list; a long A can also overlap G. | Move A's reason-surface into the Lane-3 chain, or serialize A before C, or extend the exclusivity rule to views/image.ts with an explicit order. |
| OP-D04 | HIGH | §7.2 R-43 vs §7.1 R-11, §2 depends-on | Stufe E gets an acceptance oracle that cannot run at E's position: the 0/15-clipped target is D3's post-condition, but D3 ‖ E is permitted. | Record E depends-on = B, D3 (Lane 1 = A → B → D3 → E → F), update §2 and Lane-2 text; E re-pins D3 numbers its σ_B change moves. |
| OP-D05 | HIGH | §7.1 R-02 (+R-39) vs §3 F acceptance | The robust-peak guard's discriminant is degenerate (σ=1 beam ratio ≈0.61 vs hot-pixel ≈0.10 — same sign) and the failure action is unspecified; natural fallback to rawPeak restores today's behaviour precisely in the V5b case; F's acceptance then cannot pass. | Key σ-awareness on the fitted geometry available at the call site (median-filtered peak only above a calibrated σ_minor, else σ-scaled window or model peak) and state the fallback action explicitly. |
| OP-D06 | MED | §7.1 R-21 / §7.3 C; state.ts:113-147, main.ts:1638/:1848 | Missing scope file: ImageTabState stores no dtype for the main image and main.ts discards it at load; C must persist the decoded dtype → state.ts, which appears only in Stufe G's scope. | Add state.ts to Stufe C, or drop the equality guard and keep dimension check + R-35 float32 cast parity. |
| OP-D07 | MED | §7.1 R-06 vs R-13/§4a; S18_GATE_CALIBRATION_SPEC.md:731 | The named calibration population cannot produce the measurement: the 74 clean scenes carry no non-finite pixels and yield zero bias samples by construction; only the benign random-mask family can supply a maximum. | Name the population explicitly (maximum over the benign random-mask family); keep the 74-set as the I-3 no-entry/bit-identity control. |
| OP-D08 | MED | §7.1 R-23 vs §1 I-1 / §3 F Stop-and-Report | New pins with no governing invariant: I-1 and F's HOLD trigger still govern only SNR 100/20; a drop in the new SNR-50/30 cells reads as re-pinnable rather than HOLD-worthy. | Extend I-1 and F's Stop-and-Report to all four cells with their documented targets (15/15 · 14/15 · 13/15 · 13/15). |
| OP-D09 | MED | §7.1 R-10 + §7.2 R-32; package.json test glob | The versioned oracle corpus has no runner and sits outside the green-suite rule (npm test does not glob agents/verification/s20-repros/). | Add a verify:s20repros script in 0b and name it in each stage's acceptance, or place the ported fixtures under tests/unit/. |
| OP-D10 | MED | §7.1 R-10/R-14/R-15; verify-headless.mjs:104; expected-headless-summary.json | Artifact ownership is policy-only; no stage's scope list names the artifacts; warningCount is severity-blind, so B's three new INFO codes move the pinned warningCount: 4. | Add expected-headless-summary.json and image_analyzer_cases.json to the scope rows of A, B, E, F. |
| OP-D11 | MED | §7 header table; repo grep | No review-leg report is archived, so the severity mapping is unauditable (Codex 12 HIGH vs four HIGH in adjudication; G-F1 HIGH → R-11 MED; no downgrade rationale recorded). | Archive the three leg reports (e.g. Plan/S20_PLAN_REVIEW R1..R3.md) and add a one-line reconciliation wherever a leg's HIGH lands below HIGH. |
| OP-D12 | MED | §7.2 R-32 vs §7.4.1; git show 9db4fbd | R-32 says the corpus is "Teil der Baseline", but §7.4.1 records 0a as executed without agents/verification/s20-repros/ — the clause is falsified by the operator decision printed two sections later. | Reword R-32 to "eigener Commit nach 0a". |
| OP-D13 | LOW | §7.1 R-09 vs §3 F scope / §3 H design | R-09 sound and verified, but leaves stale entries: F's scope still lists index.ts; H's design still says fitGeometryIsReleasable(params, aktuellerROI) — a result field is necessarily the analysis-ROI verdict. | Drop index.ts from F; restate H as consuming fits.gauss2d.geometryReleasable (analysis-ROI verdict). |
| OP-D14 | LOW | §2 row D3, §3 D3/H/B | Struck/changed content still reads as binding in four places (D3 title carries V6a; premises cite roi.ts tie-break lines; H carries the full-frame addendum; B calls IMAGE_ALPHA_GATE_WEAK a "Warnung"). | One mechanical fold-in pass over §2/§3, or an explicit banner "§2/§3 historisch; §7.3 ist der operative Schnitt". |
| OP-D15 | LOW | §7.1 R-14 vs §3 B Stop-and-Report / §4c | "ersetzt/ergänzt" leaves the sole rollback trigger for the ABSORBED_POWER_MIN_FRACTION change undefined. | Say "ergänzt": keep 0/74 as the hard rollback trigger, add the σ-sweep. |
| OP-D16 | LOW | §7.2 R-41, R-44 | Two revisions leave the choice open: R-41 "erzwingt (oder bietet prominent)" implies two different e2e oracles; R-44 requires a robust component peak in roi.ts while robust-peak semantics are specified only for aperture.ts. | Pick one for R-41; point R-44 at R-39's definition or give roi.ts its own. |

Spot-checks that came back clean (code-verified): R-31/§7.4.1 fully TRUE (9db4fbd: 87 files, +28 842/−89, packages/image complete, zero Orchestrierung memos, .gitignore gains); R-13 TRUE and coherent with R-30; R-09 TRUE (module-private predicate, additive boolean placeable, barrel untouched); R-10's D3 pin correction TRUE; R-04 TRUE (full-frame exemption documented at main.ts:2907-2913); R-08/R-03 anchors TRUE; R-35/R-45 executable; E's curve immunity TRUE (σ_B passed explicitly at image-aperture.test.ts:701); R-45 version claim TRUE (package.json 1.1.0, package-lock root 1.0.0); 0b executable (the three scratch corpora exist on disk and are portable).

Verdikt: NEEDS-REVISION.

**Doc-Version:** 1.0, 2026-08-23.
